# /lamina
Use: lamina-core, lamina-artifacts, lamina-guardrails.

Required checkpoints:
- framing checkpoint
- synthesis validation checkpoint
- task commit checkpoint

Artifact contract: write/reuse `.lamina/*` outputs.

Do not implement product code; generate UX artifacts/tasks only.
