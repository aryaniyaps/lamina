# Markdown Heavy Handoff

```tsx
export function Example() {
  return <main>Rendered markdown should keep this readable.</main>;
}
```

| File | Action |
| --- | --- |
| `StudioApp.tsx` | Verify run switcher state. |
| `ArtifactReviewView.tsx` | Verify rendered markdown body. |
