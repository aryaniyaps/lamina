# Developer Handoff: Complete Visual Run

## Implementation Notes

| Area | Expected behavior |
| --- | --- |
| Run switcher | Switching runs refreshes flow, persona, artifact, and screen state. |
| Persona dock | Persona changes update blocker markers in React Flow. |
| Markdown | Tables render as HTML tables and Mermaid remains visible as source. |

```ts
export const visualFixture = 'sample-visual-complete';
```
