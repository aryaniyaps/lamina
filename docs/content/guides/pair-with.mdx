# Pair Lamina with coding tools

Use Lamina to publish product behavior, query an implementation projection for your coding tool, then publish runtime verification evidence. The projection must cite its GraphVersion and never becomes canonical input.
