# Pair with Spec Kit

Generate a Spec Kit input as a read-only query projection of a resolved GraphVersion. Implement normally, then use `/lamina-verify` to execute Persona Missions. Do not import generated specification prose back as graph truth.
