# Quickstart

```bash
/lamina-init
/lamina-design
lamina graph status
# implement the resolved GraphVersion in a normal coding session
/lamina-verify
```

Run `npm run graph:observe` after source changes. Use `lamina graph diff --base main --head HEAD` for a semantic branch diff.
