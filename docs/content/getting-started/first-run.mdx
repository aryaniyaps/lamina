# First graph session

Run `/lamina-init`, then `/lamina-design` with a concrete workflow. Inspect the published result with `lamina graph status` and `lamina graph query --at HEAD`.

Application source is implemented separately. `/lamina-verify` then compiles every active Persona Mission, stages normalized runtime evidence in independent sessions, and explicitly publishes the accepted sessions.
