#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REAL_CODEX="${LAMINA_REAL_CODEX:-$HOME/.local/bin/codex}"

export LAMINA_EVAL_BIN="$SCRIPT_DIR"
export ZDOTDIR="$SCRIPT_DIR/shell-env"

exec "$REAL_CODEX" "$@"
