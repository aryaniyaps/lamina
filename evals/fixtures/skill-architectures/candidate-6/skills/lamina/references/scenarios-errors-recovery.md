# scenarios errors recovery

This is a routing index. Select a leaf from the problem signal; do not load the pack wholesale.

| Signal | Reference | Purpose |
|---|---|---|
| `edge_cases` | [edge-cases](scenarios-errors-recovery/edge-cases.md) | Derive distinct product-risk scenarios from operations, dependencies, authority, lifecycles, concurrency, destructive actions, and recovery without generating exhaustive or duplicate edge-case lists. |
| `error_handling` | [error-handling](scenarios-errors-recovery/error-handling.md) | Error and recovery UX in scenarios — slips vs mistakes, root cause in design not actors. Use when mapping failure scenarios in the active GraphVersion. |
| `empty_states` | [empty-states](scenarios-errors-recovery/empty-states.md) | Empty and zero-data UX in scenarios — first productive action when domain collections are empty. Use when collection_empty scenarios are missing. |
| `controls_and_menus` | [controls-and-menus](scenarios-errors-recovery/controls-and-menus.md) | Actions and reversibility in contracts — primary/destructive actions, undo policy, confirmations. Use when destructive ops lack scenarios. |
| `trust` | [trust](scenarios-errors-recovery/trust.md) | Trust signals in contracts — transparency for payments, irreversible actions, and sensitive data. Use when actors hesitate on verify walks or scenarios lack honesty UX. |
