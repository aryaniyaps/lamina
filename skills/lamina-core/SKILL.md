# lamina-core
Orchestrates lamina UX workflow.

Mandatory checkpoints:
- framing checkpoint
- synthesis validation checkpoint
- task commit checkpoint

Always preserve `.lamina` outputs between runs and reuse existing artifacts.

Do not implement product code; generate UX artifacts/tasks only.
