# Generated implementation contract

Generate `implement.md` from canonical `run.json` with `graph-tool.mjs render`. Do not hand-maintain a competing template.

The renderer includes active critical promises, actors and authority, entities and states, operations, workflows and terminal outcomes, invariants, distinct risk scenarios, and current scope. Deferred graph nodes are omitted.

Implementation must trace each checked id through reachable action, trusted enforcement, state mutation, durable commit, actor-scoped projection, and visible outcome or recovery. Run the repository's relevant build, tests, and behavior smoke checks before handoff.
