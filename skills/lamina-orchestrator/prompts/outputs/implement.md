# Implementation projection

Generate this human/agent-readable handoff only from a resolved GraphVersion query. Include GraphVersion and source revision, Actors/authority, Operations, Workflows, invariants, failures/recovery, Surfaces, and proof obligations.

The projection is non-canonical and must never be read back as graph state.
