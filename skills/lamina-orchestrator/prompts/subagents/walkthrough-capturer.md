# Walkthrough adapter

Use the supplied Mission closure and isolated adapter context. Emit normalized action, state, outcome, oracle, denial, recovery, artifact, budget, and capability events. Store large artifacts in the evidence CAS and return digest/locator pairs.

Do not mutate graph state directly or claim approval.
