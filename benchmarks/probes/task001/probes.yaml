task_id: task001
version: 1.0.0
probes:
  - id: inv_one_active_budget_per_household
    kind: code_guard
    description: "Invariant \"one active budget per household\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(one[\\s_-]*active[\\s_-]*budget[\\s_-]*per[\\s_-]*household).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(one[\\s_-]*active[\\s_-]*budget[\\s_-]*per[\\s_-]*household)"
  - id: inv_partner_privacy_boundary
    kind: code_guard
    description: "Invariant \"partner privacy boundary\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(partner[\\s_-]*privacy[\\s_-]*boundary).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(partner[\\s_-]*privacy[\\s_-]*boundary)"
  - id: inv_no_investment_advice_display
    kind: code_guard
    description: "Invariant \"no investment advice display\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(investment[\\s_-]*advice[\\s_-]*display).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(investment[\\s_-]*advice[\\s_-]*display)"
  - id: ent_household
    kind: entity_model
    description: "Entity \"household\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*household|(?is)household[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_account
    kind: entity_model
    description: "Entity \"account\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*account|(?is)account[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_transaction
    kind: entity_model
    description: "Entity \"transaction\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*transaction|(?is)transaction[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_budget
    kind: entity_model
    description: "Entity \"budget\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*budget|(?is)budget[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_category
    kind: entity_model
    description: "Entity \"category\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*category|(?is)category[\\s\\w]*({|struct|class|interface|=)"
  - id: scn_sync_failure_recovery
    kind: scenario_handler
    description: "Scenario/edge \"sync failure recovery\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(sync[\\s_-]*failure[\\s_-]*recovery).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(sync[\\s_-]*failure[\\s_-]*recovery)"
  - id: scn_zero_income_month
    kind: scenario_handler
    description: "Scenario/edge \"zero income month\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(zero[\\s_-]*income[\\s_-]*month).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(zero[\\s_-]*income[\\s_-]*month)"
  - id: scn_duplicate_transaction_handling
    kind: scenario_handler
    description: "Scenario/edge \"duplicate transaction handling\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(duplicate[\\s_-]*transaction[\\s_-]*handling).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(duplicate[\\s_-]*transaction[\\s_-]*handling)"
  - id: scn_empty_accounts
    kind: scenario_handler
    description: "Scenario/edge \"empty accounts\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(empty[\\s_-]*accounts).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(empty[\\s_-]*accounts)"
  - id: scn_sync_failure
    kind: scenario_handler
    description: "Scenario/edge \"sync failure\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(sync[\\s_-]*failure).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(sync[\\s_-]*failure)"
  - id: scn_duplicate_transactions
    kind: scenario_handler
    description: "Scenario/edge \"duplicate transactions\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(duplicate[\\s_-]*transactions).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(duplicate[\\s_-]*transactions)"
  - id: has_executable_slice
    kind: code_guard
    description: Contains function/class/export definitions
    weight: 1
    patterns:
      - "\\b(export\\s+(async\\s+)?function|export\\s+class|def\\s+\\w+|function\\s+\\w+|class\\s+\\w+)\\b"
