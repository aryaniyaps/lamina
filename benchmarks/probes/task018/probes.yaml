task_id: task018
version: 1.0.0
probes:
  - id: inv_downgrade_effective_end_of_period
    kind: code_guard
    description: "Invariant \"downgrade effective end of period\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(downgrade[\\s_-]*effective[\\s_-]*end[\\s_-]*period).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(downgrade[\\s_-]*effective[\\s_-]*end[\\s_-]*period)"
  - id: inv_proration_on_upgrade
    kind: code_guard
    description: "Invariant \"proration on upgrade\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(proration[\\s_-]*upgrade).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(proration[\\s_-]*upgrade)"
  - id: inv_grace_period_before_suspension
    kind: code_guard
    description: "Invariant \"grace period before suspension\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(grace[\\s_-]*period[\\s_-]*before[\\s_-]*suspension).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(grace[\\s_-]*period[\\s_-]*before[\\s_-]*suspension)"
  - id: ent_subscription
    kind: entity_model
    description: "Entity \"subscription\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*subscription|(?is)subscription[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_plan
    kind: entity_model
    description: "Entity \"plan\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*plan|(?is)plan[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_invoice
    kind: entity_model
    description: "Entity \"invoice\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*invoice|(?is)invoice[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_payment_method
    kind: entity_model
    description: "Entity \"payment method\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*payment[\\s_-]*method|(?is)payment[\\s_-]*method[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_usage_addon
    kind: entity_model
    description: "Entity \"usage addon\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*usage[\\s_-]*addon|(?is)usage[\\s_-]*addon[\\s\\w]*({|struct|class|interface|=)"
  - id: scn_card_expired_recovery
    kind: scenario_handler
    description: "Scenario/edge \"card expired recovery\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(card[\\s_-]*expired[\\s_-]*recovery).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(card[\\s_-]*expired[\\s_-]*recovery)"
  - id: scn_chargeback_handling
    kind: scenario_handler
    description: "Scenario/edge \"chargeback handling\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(chargeback[\\s_-]*handling).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(chargeback[\\s_-]*handling)"
  - id: scn_plan_discontinued_migration
    kind: scenario_handler
    description: "Scenario/edge \"plan discontinued migration\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(plan[\\s_-]*discontinued[\\s_-]*migration).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(plan[\\s_-]*discontinued[\\s_-]*migration)"
  - id: scn_usage_overage_billing
    kind: scenario_handler
    description: "Scenario/edge \"usage overage billing\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(usage[\\s_-]*overage[\\s_-]*billing).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(usage[\\s_-]*overage[\\s_-]*billing)"
  - id: scn_card_expired
    kind: scenario_handler
    description: "Scenario/edge \"card expired\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(card[\\s_-]*expired).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(card[\\s_-]*expired)"
  - id: scn_chargeback
    kind: scenario_handler
    description: "Scenario/edge \"chargeback\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(chargeback).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(chargeback)"
  - id: scn_plan_discontinued
    kind: scenario_handler
    description: "Scenario/edge \"plan discontinued\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(plan[\\s_-]*discontinued).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(plan[\\s_-]*discontinued)"
  - id: scn_usage_overage
    kind: scenario_handler
    description: "Scenario/edge \"usage overage\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(usage[\\s_-]*overage).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(usage[\\s_-]*overage)"
  - id: has_executable_implementation
    kind: code_guard
    description: Contains function/class/export definitions
    weight: 1
    patterns:
      - "\\b(export\\s+(async\\s+)?function|export\\s+class|def\\s+\\w+|function\\s+\\w+|class\\s+\\w+)\\b"
