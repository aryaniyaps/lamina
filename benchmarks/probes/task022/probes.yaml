task_id: task022
version: 1.0.0
probes:
  - id: inv_no_color_only_status
    kind: code_guard
    description: "Invariant \"no color only status\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(color[\\s_-]*only[\\s_-]*status).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(color[\\s_-]*only[\\s_-]*status)"
  - id: inv_text_alternative_for_every_chart
    kind: code_guard
    description: "Invariant \"text alternative for every chart\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(text[\\s_-]*alternative[\\s_-]*for[\\s_-]*every[\\s_-]*chart).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(text[\\s_-]*alternative[\\s_-]*for[\\s_-]*every[\\s_-]*chart)"
  - id: inv_focus_order_logical
    kind: code_guard
    description: "Invariant \"focus order logical\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(focus[\\s_-]*order[\\s_-]*logical).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(focus[\\s_-]*order[\\s_-]*logical)"
  - id: ent_dashboard
    kind: entity_model
    description: "Entity \"dashboard\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*dashboard|(?is)dashboard[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_chart
    kind: entity_model
    description: "Entity \"chart\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*chart|(?is)chart[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_filter
    kind: entity_model
    description: "Entity \"filter\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*filter|(?is)filter[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_data_table
    kind: entity_model
    description: "Entity \"data table\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*data[\\s_-]*table|(?is)data[\\s_-]*table[\\s\\w]*({|struct|class|interface|=)"
  - id: scn_live_region_overflow_handling
    kind: scenario_handler
    description: "Scenario/edge \"live region overflow handling\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(live[\\s_-]*region[\\s_-]*overflow[\\s_-]*handling).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(live[\\s_-]*region[\\s_-]*overflow[\\s_-]*handling)"
  - id: scn_empty_chart_accessible_state
    kind: scenario_handler
    description: "Scenario/edge \"empty chart accessible state\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(empty[\\s_-]*chart[\\s_-]*accessible[\\s_-]*state).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(empty[\\s_-]*chart[\\s_-]*accessible[\\s_-]*state)"
  - id: scn_loading_state_announced
    kind: scenario_handler
    description: "Scenario/edge \"loading state announced\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(loading[\\s_-]*state[\\s_-]*announced).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(loading[\\s_-]*state[\\s_-]*announced)"
  - id: scn_live_region_overflow
    kind: scenario_handler
    description: "Scenario/edge \"live region overflow\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(live[\\s_-]*region[\\s_-]*overflow).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(live[\\s_-]*region[\\s_-]*overflow)"
  - id: scn_empty_chart
    kind: scenario_handler
    description: "Scenario/edge \"empty chart\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(empty[\\s_-]*chart).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(empty[\\s_-]*chart)"
  - id: scn_loading_state
    kind: scenario_handler
    description: "Scenario/edge \"loading state\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(loading[\\s_-]*state).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(loading[\\s_-]*state)"
  - id: has_executable_slice
    kind: code_guard
    description: Contains function/class/export definitions
    weight: 1
    patterns:
      - "\\b(export\\s+(async\\s+)?function|export\\s+class|def\\s+\\w+|function\\s+\\w+|class\\s+\\w+)\\b"
