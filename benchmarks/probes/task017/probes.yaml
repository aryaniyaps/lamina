task_id: task017
version: 1.0.0
probes:
  - id: inv_receipt_required_above_threshold
    kind: code_guard
    description: "Invariant \"receipt required above threshold\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(receipt[\\s_-]*required[\\s_-]*above[\\s_-]*threshold).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(receipt[\\s_-]*required[\\s_-]*above[\\s_-]*threshold)"
  - id: inv_audit_trail_immutable
    kind: code_guard
    description: "Invariant \"audit trail immutable\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(audit[\\s_-]*trail[\\s_-]*immutable).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(audit[\\s_-]*trail[\\s_-]*immutable)"
  - id: inv_no_payment_without_finance_approval
    kind: code_guard
    description: "Invariant \"no payment without finance approval\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(payment[\\s_-]*without[\\s_-]*finance[\\s_-]*approval).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(payment[\\s_-]*without[\\s_-]*finance[\\s_-]*approval)"
  - id: ent_expense
    kind: entity_model
    description: "Entity \"expense\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*expense|(?is)expense[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_approval
    kind: entity_model
    description: "Entity \"approval\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*approval|(?is)approval[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_receipt
    kind: entity_model
    description: "Entity \"receipt\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*receipt|(?is)receipt[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_policy
    kind: entity_model
    description: "Entity \"policy\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*policy|(?is)policy[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_payment
    kind: entity_model
    description: "Entity \"payment\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*payment|(?is)payment[\\s\\w]*({|struct|class|interface|=)"
  - id: scn_policy_violation_escalation
    kind: scenario_handler
    description: "Scenario/edge \"policy violation escalation\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(policy[\\s_-]*violation[\\s_-]*escalation).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(policy[\\s_-]*violation[\\s_-]*escalation)"
  - id: scn_manager_ooo_delegation
    kind: scenario_handler
    description: "Scenario/edge \"manager ooo delegation\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(manager[\\s_-]*ooo[\\s_-]*delegation).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(manager[\\s_-]*ooo[\\s_-]*delegation)"
  - id: scn_duplicate_submission_idempotency
    kind: scenario_handler
    description: "Scenario/edge \"duplicate submission idempotency\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(duplicate[\\s_-]*submission[\\s_-]*idempotency).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(duplicate[\\s_-]*submission[\\s_-]*idempotency)"
  - id: scn_currency_conversion_rounding
    kind: scenario_handler
    description: "Scenario/edge \"currency conversion rounding\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(currency[\\s_-]*conversion[\\s_-]*rounding).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(currency[\\s_-]*conversion[\\s_-]*rounding)"
  - id: scn_policy_violation
    kind: scenario_handler
    description: "Scenario/edge \"policy violation\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(policy[\\s_-]*violation).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(policy[\\s_-]*violation)"
  - id: scn_manager_ooo
    kind: scenario_handler
    description: "Scenario/edge \"manager ooo\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(manager[\\s_-]*ooo).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(manager[\\s_-]*ooo)"
  - id: scn_duplicate_submission
    kind: scenario_handler
    description: "Scenario/edge \"duplicate submission\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(duplicate[\\s_-]*submission).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(duplicate[\\s_-]*submission)"
  - id: scn_currency_conversion
    kind: scenario_handler
    description: "Scenario/edge \"currency conversion\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(currency[\\s_-]*conversion).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(currency[\\s_-]*conversion)"
  - id: has_executable_implementation
    kind: code_guard
    description: Contains function/class/export definitions
    weight: 1
    patterns:
      - "\\b(export\\s+(async\\s+)?function|export\\s+class|def\\s+\\w+|function\\s+\\w+|class\\s+\\w+)\\b"
