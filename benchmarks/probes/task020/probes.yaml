task_id: task020
version: 1.0.0
probes:
  - id: inv_compliance_gate_before_system_access
    kind: code_guard
    description: "Invariant \"compliance gate before system access\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(compliance[\\s_-]*gate[\\s_-]*before[\\s_-]*system[\\s_-]*access).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(compliance[\\s_-]*gate[\\s_-]*before[\\s_-]*system[\\s_-]*access)"
  - id: inv_task_dependencies_enforced
    kind: code_guard
    description: "Invariant \"task dependencies enforced\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(task[\\s_-]*dependencies[\\s_-]*enforced).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(task[\\s_-]*dependencies[\\s_-]*enforced)"
  - id: inv_stage_ownership_clear
    kind: code_guard
    description: "Invariant \"stage ownership clear\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(stage[\\s_-]*ownership[\\s_-]*clear).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(stage[\\s_-]*ownership[\\s_-]*clear)"
  - id: ent_onboarding_checklist
    kind: entity_model
    description: "Entity \"onboarding checklist\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*onboarding[\\s_-]*checklist|(?is)onboarding[\\s_-]*checklist[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_task
    kind: entity_model
    description: "Entity \"task\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*task|(?is)task[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_stage
    kind: entity_model
    description: "Entity \"stage\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*stage|(?is)stage[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_new_hire
    kind: entity_model
    description: "Entity \"new hire\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*new[\\s_-]*hire|(?is)new[\\s_-]*hire[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_compliance_training
    kind: entity_model
    description: "Entity \"compliance training\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*compliance[\\s_-]*training|(?is)compliance[\\s_-]*training[\\s\\w]*({|struct|class|interface|=)"
  - id: scn_delayed_start_date_handling
    kind: scenario_handler
    description: "Scenario/edge \"delayed start date handling\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(delayed[\\s_-]*start[\\s_-]*date[\\s_-]*handling).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(delayed[\\s_-]*start[\\s_-]*date[\\s_-]*handling)"
  - id: scn_role_change_mid_onboarding
    kind: scenario_handler
    description: "Scenario/edge \"role change mid onboarding\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(role[\\s_-]*change[\\s_-]*mid[\\s_-]*onboarding).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(role[\\s_-]*change[\\s_-]*mid[\\s_-]*onboarding)"
  - id: scn_it_provisioning_failure_recovery
    kind: scenario_handler
    description: "Scenario/edge \"it provisioning failure recovery\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(provisioning[\\s_-]*failure[\\s_-]*recovery).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(provisioning[\\s_-]*failure[\\s_-]*recovery)"
  - id: scn_delayed_start
    kind: scenario_handler
    description: "Scenario/edge \"delayed start\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(delayed[\\s_-]*start).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(delayed[\\s_-]*start)"
  - id: scn_it_provisioning_failure
    kind: scenario_handler
    description: "Scenario/edge \"it provisioning failure\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(provisioning[\\s_-]*failure).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(provisioning[\\s_-]*failure)"
  - id: has_executable_implementation
    kind: code_guard
    description: Contains function/class/export definitions
    weight: 1
    patterns:
      - "\\b(export\\s+(async\\s+)?function|export\\s+class|def\\s+\\w+|function\\s+\\w+|class\\s+\\w+)\\b"
