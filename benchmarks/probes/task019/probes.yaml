task_id: task019
version: 1.0.0
probes:
  - id: inv_last_super_admin_protected
    kind: code_guard
    description: "Invariant \"last super admin protected\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(last[\\s_-]*super[\\s_-]*admin[\\s_-]*protected).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(last[\\s_-]*super[\\s_-]*admin[\\s_-]*protected)"
  - id: inv_permission_inheritance_transitive
    kind: code_guard
    description: "Invariant \"permission inheritance transitive\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(permission[\\s_-]*inheritance[\\s_-]*transitive).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(permission[\\s_-]*inheritance[\\s_-]*transitive)"
  - id: inv_sso_group_mapping_consistent
    kind: code_guard
    description: "Invariant \"sso group mapping consistent\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(sso[\\s_-]*group[\\s_-]*mapping[\\s_-]*consistent).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(sso[\\s_-]*group[\\s_-]*mapping[\\s_-]*consistent)"
  - id: ent_role
    kind: entity_model
    description: "Entity \"role\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*role|(?is)role[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_permission
    kind: entity_model
    description: "Entity \"permission\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*permission|(?is)permission[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_org
    kind: entity_model
    description: "Entity \"org\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*org|(?is)org[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_team
    kind: entity_model
    description: "Entity \"team\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*team|(?is)team[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_audit_log
    kind: entity_model
    description: "Entity \"audit log\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*audit[\\s_-]*log|(?is)audit[\\s_-]*log[\\s\\w]*({|struct|class|interface|=)"
  - id: scn_conflicting_permissions_resolution
    kind: scenario_handler
    description: "Scenario/edge \"conflicting permissions resolution\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(conflicting[\\s_-]*permissions[\\s_-]*resolution).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(conflicting[\\s_-]*permissions[\\s_-]*resolution)"
  - id: scn_role_deletion_with_active_users
    kind: scenario_handler
    description: "Scenario/edge \"role deletion with active users\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(role[\\s_-]*deletion[\\s_-]*with[\\s_-]*active[\\s_-]*users).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(role[\\s_-]*deletion[\\s_-]*with[\\s_-]*active[\\s_-]*users)"
  - id: scn_sso_sync_delay_handling
    kind: scenario_handler
    description: "Scenario/edge \"sso sync delay handling\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(sso[\\s_-]*sync[\\s_-]*delay[\\s_-]*handling).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(sso[\\s_-]*sync[\\s_-]*delay[\\s_-]*handling)"
  - id: scn_conflicting_permissions
    kind: scenario_handler
    description: "Scenario/edge \"conflicting permissions\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(conflicting[\\s_-]*permissions).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(conflicting[\\s_-]*permissions)"
  - id: scn_role_deletion_with_users
    kind: scenario_handler
    description: "Scenario/edge \"role deletion with users\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(role[\\s_-]*deletion[\\s_-]*with[\\s_-]*users).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(role[\\s_-]*deletion[\\s_-]*with[\\s_-]*users)"
  - id: scn_sso_sync_delay
    kind: scenario_handler
    description: "Scenario/edge \"sso sync delay\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(sso[\\s_-]*sync[\\s_-]*delay).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(sso[\\s_-]*sync[\\s_-]*delay)"
  - id: has_executable_implementation
    kind: code_guard
    description: Contains function/class/export definitions
    weight: 1
    patterns:
      - "\\b(export\\s+(async\\s+)?function|export\\s+class|def\\s+\\w+|function\\s+\\w+|class\\s+\\w+)\\b"
