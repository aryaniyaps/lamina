task_id: task025
version: 1.0.0
probes:
  - id: inv_queued_action_idempotent
    kind: code_guard
    description: "Invariant \"queued action idempotent\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(queued[\\s_-]*action[\\s_-]*idempotent).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(queued[\\s_-]*action[\\s_-]*idempotent)"
  - id: inv_bandwidth_mode_reduces_payload
    kind: code_guard
    description: "Invariant \"bandwidth mode reduces payload\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(bandwidth[\\s_-]*mode[\\s_-]*reduces[\\s_-]*payload).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(bandwidth[\\s_-]*mode[\\s_-]*reduces[\\s_-]*payload)"
  - id: inv_partial_load_recoverable
    kind: code_guard
    description: "Invariant \"partial load recoverable\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(partial[\\s_-]*load[\\s_-]*recoverable).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(partial[\\s_-]*load[\\s_-]*recoverable)"
  - id: ent_media_asset
    kind: entity_model
    description: "Entity \"media asset\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*media[\\s_-]*asset|(?is)media[\\s_-]*asset[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_sync_queue
    kind: entity_model
    description: "Entity \"sync queue\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*sync[\\s_-]*queue|(?is)sync[\\s_-]*queue[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_bandwidth_profile
    kind: entity_model
    description: "Entity \"bandwidth profile\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*bandwidth[\\s_-]*profile|(?is)bandwidth[\\s_-]*profile[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_action
    kind: entity_model
    description: "Entity \"action\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*action|(?is)action[\\s\\w]*({|struct|class|interface|=)"
  - id: scn_connection_drop_mid_upload
    kind: scenario_handler
    description: "Scenario/edge \"connection drop mid upload\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(connection[\\s_-]*drop[\\s_-]*mid[\\s_-]*upload).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(connection[\\s_-]*drop[\\s_-]*mid[\\s_-]*upload)"
  - id: scn_timeout_retry
    kind: scenario_handler
    description: "Scenario/edge \"timeout retry\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(timeout[\\s_-]*retry).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(timeout[\\s_-]*retry)"
  - id: scn_partial_load_resume
    kind: scenario_handler
    description: "Scenario/edge \"partial load resume\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(partial[\\s_-]*load[\\s_-]*resume).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(partial[\\s_-]*load[\\s_-]*resume)"
  - id: scn_timeout
    kind: scenario_handler
    description: "Scenario/edge \"timeout\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(timeout).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(timeout)"
  - id: scn_partial_load
    kind: scenario_handler
    description: "Scenario/edge \"partial load\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(partial[\\s_-]*load).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(partial[\\s_-]*load)"
  - id: has_executable_slice
    kind: code_guard
    description: Contains function/class/export definitions
    weight: 1
    patterns:
      - "\\b(export\\s+(async\\s+)?function|export\\s+class|def\\s+\\w+|function\\s+\\w+|class\\s+\\w+)\\b"
