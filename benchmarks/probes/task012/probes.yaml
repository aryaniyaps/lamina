task_id: task012
version: 1.0.0
probes:
  - id: inv_last_admin_cannot_be_removed
    kind: code_guard
    description: "Invariant \"last admin cannot be removed\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(last[\\s_-]*admin[\\s_-]*cannot[\\s_-]*removed).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(last[\\s_-]*admin[\\s_-]*cannot[\\s_-]*removed)"
  - id: inv_role_permissions_consistent
    kind: code_guard
    description: "Invariant \"role permissions consistent\" with guard/validation"
    weight: 2
    patterns:
      - "(?is)(role[\\s_-]*permissions[\\s_-]*consistent).{0,120}(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant)|(?is)(if|throw|assert|require|guard|validate|forbid|reject|cannot|must|ensure|check|deny|unauthorized|forbidden|invariant).{0,120}(role[\\s_-]*permissions[\\s_-]*consistent)"
  - id: ent_project
    kind: entity_model
    description: "Entity \"project\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*project|(?is)project[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_member
    kind: entity_model
    description: "Entity \"member\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*member|(?is)member[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_role
    kind: entity_model
    description: "Entity \"role\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*role|(?is)role[\\s\\w]*({|struct|class|interface|=)"
  - id: ent_workflow_state
    kind: entity_model
    description: "Entity \"workflow state\" in type/model context"
    weight: 1
    patterns:
      - "(?is)(type|interface|class|struct|model|schema|entity|record|enum|typedef)[\\s\\w{:,<]*workflow[\\s_-]*state|(?is)workflow[\\s_-]*state[\\s\\w]*({|struct|class|interface|=)"
  - id: scn_last_admin_removal_blocked
    kind: scenario_handler
    description: "Scenario/edge \"last admin removal blocked\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(last[\\s_-]*admin[\\s_-]*removal[\\s_-]*blocked).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(last[\\s_-]*admin[\\s_-]*removal[\\s_-]*blocked)"
  - id: scn_invalid_invite_handling
    kind: scenario_handler
    description: "Scenario/edge \"invalid invite handling\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(invalid[\\s_-]*invite[\\s_-]*handling).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(invalid[\\s_-]*invite[\\s_-]*handling)"
  - id: scn_settings_conflict_resolution
    kind: scenario_handler
    description: "Scenario/edge \"settings conflict resolution\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(settings[\\s_-]*conflict[\\s_-]*resolution).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(settings[\\s_-]*conflict[\\s_-]*resolution)"
  - id: scn_last_admin_removal
    kind: scenario_handler
    description: "Scenario/edge \"last admin removal\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(last[\\s_-]*admin[\\s_-]*removal).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(last[\\s_-]*admin[\\s_-]*removal)"
  - id: scn_invalid_invite
    kind: scenario_handler
    description: "Scenario/edge \"invalid invite\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(invalid[\\s_-]*invite).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(invalid[\\s_-]*invite)"
  - id: scn_settings_conflict
    kind: scenario_handler
    description: "Scenario/edge \"settings conflict\" near error/recovery handling"
    weight: 2
    patterns:
      - "(?is)(settings[\\s_-]*conflict).{0,160}(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue)|(?is)(catch|error|recover|retry|fallback|empty|offline|timeout|conflict|handler|onError|rescue).{0,160}(settings[\\s_-]*conflict)"
  - id: has_executable_implementation
    kind: code_guard
    description: Contains function/class/export definitions
    weight: 1
    patterns:
      - "\\b(export\\s+(async\\s+)?function|export\\s+class|def\\s+\\w+|function\\s+\\w+|class\\s+\\w+)\\b"
