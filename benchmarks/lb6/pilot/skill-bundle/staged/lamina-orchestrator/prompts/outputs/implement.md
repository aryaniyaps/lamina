# Generated implementation contract

Generate `implement.md` from canonical `run.json` with `graph-tool.mjs render`. Do not hand-maintain a competing template.

The renderer includes active critical promises; actor authority, identity proof, and reachable entry or activation path; entity identity, key field contracts, relationships, complete states or derived statuses, and lifecycle consequences; operation preconditions, trusted enforcement, state mutation, durable effects, failures, and recovery; end-to-end workflows; invariants; dependencies with concrete fulfillment and degraded behavior; surface and journey contracts; distinct-risk proof scenarios; traceability; decisions; and current scope. Deferred graph nodes are omitted.

Implementation must trace each checked id through reachable actor enrollment or activation, reachable action, trusted enforcement, state mutation, durable commit, actor-scoped projection, and visible outcome or recovery. Consequential local wall-time input must cross the client boundary as untouched local components plus its IANA zone and be resolved by a trusted timezone-aware server with explicit DST gap/overlap recovery; never parse `datetime-local` in the browser's zone and relabel it. Recurrences advance by calendar rule in their named zone and preserve rollover/catch-up behavior. User-visible history promises show who acted and the authoritative time.

If a critical dependency names an external identity or delivery platform, provide a runnable current-slice adapter and a concrete provider interface/configuration/health seam. Local adapters must remain truthful and usable; production mode must fail closed without required configuration. An email string alone is not proof of account control, and internal notification capture must never be presented as delivered. Keep authoritative in-product state independent of delivery. Cookie-authenticated mutations require an explicit same-site plus CSRF/origin strategy, and runtime-version-specific platform APIs require a declared minimum runtime.

Tests must synchronize on the authoritative post-action state, use controlled clocks for time boundaries, set browser zone different from subject zone when local time is consequential, cover applicable DST gap/overlap and recurrence rollover cases, and use separate authenticated contexts for multi-actor promises. Give every test a finite per-test timeout and release every test-owned server, worker, database, browser, context, listener, timer, and temporary resource in a `finally` block, including setup and assertion failures. Run the complete declared suite at least three times; any timeout, open-handle delay, skip, or nonzero exit is a proof failure. Run the repository's relevant build, responsive, and accessibility checks before handoff.

Implement the proof packet before adding supporting breadth. Name automated checks with the exact marker `[proof:<id>]`, create a method-neutral root `product-proof-manifest.json` mapping every proof id to existing test files, evidence levels, and test requirements, and ensure the declared test suite executes those files. A checked box, comment, type, or manifest entry without observable passing behavior is not proof.
