/lamina-init

---

{{ instruction }}

---

## Lamina treatment workflow (required — slash commands only)

Lamina command skills (`lamina-init`, `lamina-design`, `lamina-verify`) must be invoked via **slash commands** or the **Skill tool** — in workflow order — before writing application source.

**This session already started with `/lamina-init` above.** Complete init, then continue in order. **Do not write application source until after `/lamina-init` and `/lamina-design` have run.**

**Do not end the session until all three command skills have run and application source is implemented.**

1. **`/lamina-design`** — produce `run.yaml` + `implement.md` under `.lamina/runs/<run_id>/` only (no app source).
2. **Implement (coding agent, not a Lamina command)** — full product from **`run.yaml` + `implement.md`** + the task brief, in application source outside `.lamina/`. Finish end-to-end; do not stop at a scaffold or wait for proceed.
3. **`/lamina-verify`** — post-build verification; write findings under `.lamina/runs/<run_id>/` only.
4. **Fix (coding agent, not a Lamina command)** — apply product fixes from `fix.md` in application source.

Use `.lamina/runs/<run_id>/` layout (`run.yaml`, `implement.md`, `report.md`, `fix.md`). Do **not** use `.lamina/ready_to_build/` as a directory.

See `AGENTS.md` for full workflow detail.

---

## Unattended benchmark contract

This is an **unattended** benchmark run — the user cannot respond to questions.

- Treat the task brief and any fixture context in this workspace as **authoritative user input**.
- If information is missing, proceed with **labeled assumptions** documented under **Open questions** — do not ask clarifying questions or stop waiting for user input.
- If a Lamina skill would normally clarify and STOP, **do not STOP** — document assumptions and continue the workflow.
- Deliver a **complete product implementation** in this workspace: write application source files. Do not only plan, describe, or ask what to do next.
