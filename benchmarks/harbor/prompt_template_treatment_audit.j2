/lamina-init

---

{{ instruction }}

---

## Lamina treatment workflow (required — slash commands only)

Lamina command skills have **disable-model-invocation**. The harness (or user) must send each step as a **slash-command user message**.

**This session already started with `/lamina-init` above.** Complete init, then continue in order:

1. **`/lamina-verify`** — audit the existing product; write findings under `.lamina/runs/<run_id>/` only (no app source).
2. **Fix (next user turn, not a Lamina command)** — apply prioritized product fixes from `fix.md` in application source.
3. **`/lamina-verify`** again — post-fix verification under `.lamina/` only.

Use `.lamina/runs/<run_id>/` layout.

---

## Unattended trial contract

This is an **unattended** trial — the user cannot respond mid-turn. Treat the task brief and fixture context as authoritative.
