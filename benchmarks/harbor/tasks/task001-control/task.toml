schema_version = "1.3"
artifacts = []

[task]
name = "aryaniyaps/task001-control"
description = "LaminaBench task001 control arm (greenfield)"
authors = [{ name = "LaminaBench" }]
keywords = ["lamina", "ecological-matched-phases", "control", "greenfield"]

[metadata]
author_name = "LaminaBench"
category = "greenfield"
difficulty = "hard"
tags = ["lamina", "ecological-matched-phases", "control"]
lamina_task_id = "task001"
lamina_arm = "control"
lamina_workflow = "design"

[verifier]
timeout_sec = 600.0
collect = []

[verifier.env]
ANTHROPIC_API_KEY = "${ANTHROPIC_API_KEY}"
ANTHROPIC_AUTH_TOKEN = "${ANTHROPIC_AUTH_TOKEN}"
ANTHROPIC_BASE_URL = "${ANTHROPIC_BASE_URL}"
REWARDKIT_JUDGE = "${REWARDKIT_JUDGE}"
LAMINA_BENCH_RUN = "${LAMINA_BENCH_RUN}"

[agent]
timeout_sec = 5400.0

[environment]
network_mode = "public"
build_timeout_sec = 1800.0
os = "linux"
cpus = 2
memory_mb = 4096
storage_mb = 10240
mcp_servers = []

[environment.env]

[solution.env]
