# dev-brown-01 — lamina — design

/lamina-design
Create and validate the minimum sufficient implementation contract from the brief and context. Defer application source until the implementation step.

## Authoritative brief

# Commerce order follow-through

Teams using this Commerce fixture lose track of important order decisions. Add a focused feature that makes those decisions visible and ensures affected orders are not quietly left inconsistent. Fit the existing product rather than creating a separate app.


This is an unattended benchmark step. Continue with labeled assumptions when the brief is incomplete. Keep application artifacts method-neutral.
