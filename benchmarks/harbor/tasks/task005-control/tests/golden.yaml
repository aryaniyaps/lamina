task_id: task005
# Behavioral reference checklist for the LLM judge rubric (Design C Option D).
# Score product behavior, not exact wording / checklist id echo.
required_entities:
  - cart
  - order
  - payment
  - shipping_address
required_invariants:
  - order_total_matches_line_items
  - payment_required_before_confirmation
required_personas:
  - first_time_buyer
  - returning_customer
required_flows:
  - cart_review
  - shipping_entry
  - payment
  - confirmation
required_findings:
  - invariant_violation
  - state_consistency
  - permission_gap
  - error_recovery
  - idempotency_risk
required_scenarios:
  - payment_declined_recovery
  - address_validation_failure
  - session_timeout_mid_checkout
required_edge_cases:
  - payment_declined
  - address_validation_failure
  - session_timeout
required_a11y:
  - form_labels
  - error_announcement
  - keyboard_checkout
