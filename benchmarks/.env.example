# Optional bench-only overrides (not required).
# Cursor CLI auth for `npm run bench:run:*` comes from the repo root `.env`:
#
#   CURSOR_API_KEY=...
#
# Optional Claude Code fallback (`--agent claude-code`):
#
#   ANTHROPIC_API_KEY=sk-ant-...
